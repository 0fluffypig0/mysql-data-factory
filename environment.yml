name: mysql_factory_env
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - pip:
    - pymysql==1.1.2
    - sqlalchemy
    - python-dotenv==1.2.2
    - loguru==0.7.3
    - tabulate
    - tqdm
    - python-dateutil
